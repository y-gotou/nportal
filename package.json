{
  "name": "nportal",
  "type": "module",
  "private": true,
  "scripts": {
    "dev": "nuxt dev",
    "build": "nuxt build",
    "typecheck": "nuxt typecheck",
    "test": "node --experimental-strip-types --test tests/*.test.ts tests/*.test.mjs",
    "check": "npm run typecheck && npm run build",
    "preview": "npm run build && wrangler pages dev dist",
    "db:seed:local": "wrangler d1 execute nportal-db --local --file db/schema.sql && wrangler d1 execute nportal-db --local --file db/seed.sql",
    "db:seed:prod": "wrangler d1 execute nportal-db --remote --file db/schema.sql && wrangler d1 execute nportal-db --remote --file db/seed.sql",
    "db:seed:preview": "wrangler d1 execute nportal-preview-db --remote --file db/schema.sql && wrangler d1 execute nportal-preview-db --remote --file db/seed.sql",
    "db:migrate:local": "wrangler d1 execute nportal-db --local --file db/migrate.sql",
    "db:migrate:prod": "wrangler d1 execute nportal-db --remote --file db/migrate.sql",
    "db:migrate:preview": "wrangler d1 execute nportal-preview-db --remote --file db/migrate.sql",
    "postinstall": "nuxt prepare"
  },
  "dependencies": {
    "nuxt": "^4.4.2",
    "remark": "^15.0.1",
    "remark-gfm": "^4.0.1",
    "remark-html": "^16.0.1",
    "vue": "^3.5.30",
    "vue-router": "^5.0.4"
  },
  "devDependencies": {
    "@tailwindcss/vite": "^4.2.2",
    "@types/node": "^25.5.0",
    "nitro-cloudflare-dev": "^0.2.2",
    "tailwindcss": "^4.2.2",
    "typescript": "^5.9.3",
    "vue-tsc": "^3.1.1",
    "wrangler": "^4.78.0"
  }
}
